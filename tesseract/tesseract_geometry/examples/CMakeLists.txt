find_package(tesseract_support REQUIRED)

add_executable(${PROJECT_NAME}_create_geometries_example create_geometries_example.cpp)
target_link_libraries(${PROJECT_NAME}_create_geometries_example ${PROJECT_NAME} console_bridge tesseract::tesseract_support)
target_compile_options(${PROJECT_NAME}_create_geometries_example PRIVATE -Wsuggest-override -Wconversion -Wsign-conversion)
if(CXX_FEATURE_FOUND EQUAL "-1")
    target_compile_options(${PROJECT_NAME}_create_geometries_example PRIVATE -std=c++11)
else()
    target_compile_features(${PROJECT_NAME}_create_geometries_example PRIVATE cxx_std_11)
endif()
if(CMAKE_CXX_COMPILER_ID STREQUAL "GNU")
    target_compile_options(${PROJECT_NAME}_create_geometries_example PRIVATE -mno-avx)
  else()
    message(WARNING "Non-GNU compiler detected. If using AVX instructions, Eigen alignment issues may result.")
  endif()

install(TARGETS ${PROJECT_NAME}_create_geometries_example DESTINATION bin)

add_executable(${PROJECT_NAME}_parse_mesh_example parse_mesh_example.cpp)
target_link_libraries(${PROJECT_NAME}_parse_mesh_example ${PROJECT_NAME} console_bridge tesseract::tesseract_support)
target_compile_options(${PROJECT_NAME}_parse_mesh_example PRIVATE -Wsuggest-override -Wconversion -Wsign-conversion)
if(CXX_FEATURE_FOUND EQUAL "-1")
    target_compile_options(${PROJECT_NAME}_parse_mesh_example PRIVATE -std=c++11)
else()
    target_compile_features(${PROJECT_NAME}_parse_mesh_example PRIVATE cxx_std_11)
endif()
if(CMAKE_CXX_COMPILER_ID STREQUAL "GNU")
    target_compile_options(${PROJECT_NAME}_parse_mesh_example PRIVATE -mno-avx)
  else()
    message(WARNING "Non-GNU compiler detected. If using AVX instructions, Eigen alignment issues may result.")
  endif()

install(TARGETS ${PROJECT_NAME}_parse_mesh_example DESTINATION bin)
